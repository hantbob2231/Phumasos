{
  "name": "figureos-webos",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch server/server.js",
    "start": "node server/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=20" }
}
