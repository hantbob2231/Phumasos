export function renderFilesApp(root){root.innerHTML=`<div class="file-drop"><h2 style="margin:0 0 8px">Files</h2><p style="color:#888;font-size:10px">Session-only file shelf. FigureOS does not upload these files.</p><input id="files-input" type="file" multiple></div><div id="files-list" class="file-list"><div class="search-empty">No files selected.</div></div>`;const input=root.querySelector('#files-input'),list=root.querySelector('#files-list');input.onchange=()=>{const files=[...input.files];list.innerHTML=files.length?files.map(f=>`<div class="file-row"><span>${escapeHtml(f.name)}</span><span>${(f.size/1024).toFixed(1)} KB • ${escapeHtml(f.type||'file')}</span></div>`).join(''):'<div class="search-empty">No files selected.</div>';};}
function escapeHtml(s=''){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
